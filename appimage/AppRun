#!/bin/bash
# RIT.AppImage — runs RIT in the bundled stock-WineHQ userspace via bubblewrap.
#
# The whole working Wine 11 + .NET + RIT userspace (a Debian rootfs) is bundled, so
# the AppImage runs on ANY 64-bit Linux host with zero dependencies — no 32-bit
# libs, no winex11 whack-a-mole. bwrap (from inside the rootfs, invoked via the
# rootfs's own loader so it needs nothing from the host) maps the rootfs as / and
# binds in the host's X socket. Display is the host's native X (or XWayland) — a
# real window, no VNC. Teardown: --die-with-parent + --unshare-pid means the
# kernel reaps every process (wineserver, services…) when the launcher dies,
# however it dies (incl. Force Quit). Requires unprivileged user namespaces
# (default on all modern distros).
set -u
HERE="$(dirname "$(readlink -f "$0")")"
R="$HERE/rootfs"

# Writable, persistent prefix in the user's home (first run: seed from the bundle).
DATA="${XDG_DATA_HOME:-$HOME/.local/share}/RIT"
PFX="$DATA/prefix"
if [ ! -f "$PFX/.rit-ready" ]; then
    echo "First run: setting up RIT (one-time)…" >&2
    mkdir -p "$DATA"; rm -rf "$PFX"
    cp -a "$R/opt/rit-prefix" "$PFX"
    touch "$PFX/.rit-ready"
fi

# Invoke the rootfs's bwrap through the rootfs's own dynamic loader, so it depends
# on nothing from the host (the host only needs a kernel with user namespaces).
LD="$R/lib64/ld-linux-x86-64.so.2"
LP="$R/lib/x86_64-linux-gnu:$R/usr/lib/x86_64-linux-gnu:$R/lib64:$R/usr/lib"

exec "$LD" --library-path "$LP" "$R/usr/bin/bwrap" \
    --bind "$R" / \
    --bind "$PFX" /opt/rit-prefix \
    --dev /dev --proc /proc --tmpfs /tmp --tmpfs /run --tmpfs /dev/shm \
    --ro-bind-try /tmp/.X11-unix /tmp/.X11-unix \
    --ro-bind-try "${XAUTHORITY:-$HOME/.Xauthority}" /root/.Xauthority \
    --ro-bind-try /etc/resolv.conf /etc/resolv.conf \
    --setenv HOME /root \
    --setenv DISPLAY "${DISPLAY:-:0}" \
    --setenv XAUTHORITY /root/.Xauthority \
    --setenv WINEPREFIX /opt/rit-prefix \
    --setenv WINEARCH win32 \
    --setenv WINEDEBUG -all \
    --setenv WINEDLLOVERRIDES "mscoree=n;mshtml=d" \
    --unshare-pid --die-with-parent \
    /usr/bin/wine "C:\\Program Files\\Rotman\\RIT User Application\\Client.exe" "$@"
